{
	"shell_cmd": ["sh", "-x", "$file"],
    "file_regex": "^(..[^:]*):([0-9]+):?([0-9]+)?:? (.*)$",
	"working_dir": "${file_path}",
	"selector": "source.shell",

	"variants":
	[
		{
			"name": "Run",
			
			"shell_cmd": "sh -x \"${file}\""
		}
	]
}
